import { TooltipDirective } from './tooltip';

describe('Tooltip', () => {
  it('should create an instance', () => {
    const directive = new TooltipDirective('' as any);
    expect(directive).toBeTruthy();
  });
});
